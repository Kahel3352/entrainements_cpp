#include "person.h"

int main()
{
    //instantiation of p (a Person)
	Person p;

    //setting the name of p
	p.setName("b");

    //displaying the name of p
	cout<<p.getName()<<endl;

    //displaying of the age of p
	cout<<p.getAge()<<endl;

    //ask user to define the person
	p.input();

    //show some informations about the person
	p.display();

    //try with another instantiation
    Person a;
    p.setName("a"); 
	cout<<p.getName()<<endl;
	cout<<p.getAge()<<endl;
	p.input();
	p.display();
}
